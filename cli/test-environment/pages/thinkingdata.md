# ThinkingData connector smoke

Game DAU / wiki pages belong in the Self-Data `evidence/` project. This page only checks the OpenAPI handshake.

```sql ping
SELECT 1 AS ok
```

{% table data="ping" /%}
